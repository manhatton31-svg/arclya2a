{
  "name": "createMonitor",
  "description": "Create a new Axiom monitor using a JSON payload for Threshold, MatchEvent, or AnomalyDetection. Provide name, type, intervalMinutes, rangeMinutes, notifierIds, and at least one of aplQuery or mplQuery.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "monitorJson": {
        "type": "string",
        "minLength": 1,
        "description": "The full monitor configuration as a JSON string. Must include name, type, intervalMinutes, rangeMinutes, and at least one of aplQuery or mplQuery."
      }
    },
    "required": [
      "monitorJson"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}