{
  "name": "exportDashboard",
  "description": "Export a dashboard as JSON by its UID. Returns the full dashboard document.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "dashboardUid": {
        "type": "string",
        "description": "The dashboard UID (stable external identifier). You can find UIDs using the `listDashboards()` tool."
      }
    },
    "required": [
      "dashboardUid"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}